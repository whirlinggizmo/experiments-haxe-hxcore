# haxelib convention.  haxelib will automatically append extraParams.hxml when the library is included

# Register components macro
--macro hxcore.flecs.flecs_wrapper.bindings.haxe.ComponentMacro.register()

# Make it.each(...) extension available by default
--macro hxcore.flecs.SystemIterMacro.register()
